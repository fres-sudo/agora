name: feature_onboarding
description: First-run onboarding wizard and business-type setup for Agora
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.10.0 <4.0.0'
  flutter: '>=3.38.5'

dependencies:
  auth_session:
    path: ../../packages/auth_session
  auto_route: ^11.1.0
  bloc_exports:
    path: ../../packages/bloc
  database:
    path: ../../packages/database
  drift: ^2.30.0
  feature_flags:
    path: ../../packages/feature_flags
  flutter:
    sdk: flutter
  talker: ^5.1.9
  ui_kit:
    path: ../../packages/ui_kit
  utils:
    path: ../../packages/utils

dev_dependencies:
  auto_route_generator: ^10.2.6
  build_runner: ^2.4.7
  flutter_test:
    sdk: flutter

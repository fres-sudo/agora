name: feature_auth
description: Authentication and session management feature
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.10.0 <4.0.0'
  flutter: '>=3.38.5'

dependencies:
  auto_route: ^11.1.0
  bloc_exports:
    path: ../../packages/bloc
  database:
    path: ../../packages/database
  drift: ^2.30.0
  errors:
    path: ../../packages/errors
  flutter:
    sdk: flutter
  flutter_secure_storage: ^10.0.0
  logger:
    path: ../../packages/logger
  result:
    path: ../../packages/result
  talker: ^5.1.9
  theme: ^0.0.0
  ui_kit:
    path: ../../packages/ui_kit

dev_dependencies:
  auto_route_generator: ^10.2.6
  build_runner: ^2.4.7
  drift_dev: ^2.30.0
  flutter_test:
    sdk: flutter
  freezed: ^3.2.3

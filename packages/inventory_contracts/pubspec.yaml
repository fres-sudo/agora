name: inventory_contracts
description: Shared inventory/stock repository interface for Agora
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.10.0 <4.0.0'
  flutter: '>=3.38.5'

dependencies:
  flutter:
    sdk: flutter
  result:
    path: ../result

dev_dependencies:
  flutter_test:
    sdk: flutter

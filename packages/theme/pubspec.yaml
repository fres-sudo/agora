name: theme
description: App theme, ThemeCubit and device utilities for Agora
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.10.0 <4.0.0'
  flutter: '>=3.38.5'

dependencies:
  bloc: ^9.0.0
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  freezed_annotation: ^3.1.0
  google_fonts: ^6.3.1
  utils:
    path: ../utils

dev_dependencies:
  flutter_test:
    sdk: flutter

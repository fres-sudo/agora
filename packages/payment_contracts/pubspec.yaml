name: payment_contracts
description: Payment terminal contracts shared by Agora features and the app shell
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.10.0 <4.0.0'

dev_dependencies:
  test: ^1.25.8
